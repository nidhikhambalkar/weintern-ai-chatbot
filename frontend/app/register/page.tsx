import LeadForm from "@/components/LeadForm";

export default function RegisterPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-slate-50 p-3 sm:p-6 overflow-x-hidden">
      <div className="w-full max-w-md my-auto">
        <LeadForm />
      </div>
    </main>
  );
}
